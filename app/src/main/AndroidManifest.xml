<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.haiqal.crudsqlitehaiqallanjutan">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/kampus"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".perpustakaan" />
        <activity android:name=".instagram" />
        <activity android:name=".twitter" />
        <activity android:name=".facebook" />
        <activity android:name=".formulir" />
        <activity android:name=".anggota" />
        <activity android:name=".mylibrary" />
        <activity android:name=".kategori" />
        <activity android:name=".news" />
        <activity android:name=".multimedia" />
        <activity android:name=".contactus" />
        <activity android:name=".perpusmahasiswa" />
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login" />
        <activity android:name=".signup" />
        <activity android:name=".resetpassword" />
        <activity android:name=".formlogin2"></activity>
    </application>

</manifest>